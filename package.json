{
  "name": "server",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "yarn dev",
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build",
    "lint": "echo 1",
    "types": "tsc -p tsconfig.json --noEmit",
    "commit": "cz",
    "unit": "CI=true yarn workspaces run unit",
    "integration": "CI=true yarn workspaces run integration",
    "api": "CI=true yarn workspaces run api",
    "test": "yarn unit && yarn integration && yarn api",
    "coverage": "CI=true yarn workspaces run coverage",
    "env": "CI=true yarn workspaces run env",
    "up": "yarn workspaces run up",
    "pull": "yarn workspaces run pull",
    "bootstrap": "yarn workspaces run bootstrap"
  },
  "dependencies": {
    "rimraf": "3.0.2"
  },
  "devDependencies": {
    "@commitlint/cli": "16.2.4",
    "@commitlint/config-conventional": "16.2.4",
    "@typescript-eslint/eslint-plugin": "5.30.6",
    "@typescript-eslint/parser": "5.30.6",
    "commitizen": "4.2.4",
    "cz-conventional-changelog": "3.3.0",
    "eslint-plugin-prettier": "4.2.1",
    "husky": "8.0.1",
    "lint-staged": "12.4.1",
    "prettier": "2.6.2",
    "pretty-quick": "3.1.3",
    "typescript": "4.6.4"
  }
}
